library(shiny) #make sure you install.packages("shiny") in the R terminal

ui <- fluidPage()

server <- function(input, output) {}

shinyApp(ui, server) #copy this into terminal and paste after playing this file if it doesn't open a file 
